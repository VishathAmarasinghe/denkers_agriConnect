/** @type {import('next').NextConfig} */
const nextConfig = {
  // Port configuration is handled in package.json scripts
  // No need for experimental.appDir as it's now default
}

module.exports = nextConfig
